{
  "name": "austere-innocent-dogfish",
  "version": "1.0.0",
  "private": true,
  "homepage": "https://adzelfirestar.github.io/velocitybroadcast-reborn/",
  "dependencies": {
    "@craco/craco": "^7.1.0",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-router-dom": "^5.2.0",
    "react-helmet": "^6.1.0",
    "dangerous-html": "0.1.13"
    // Optionally remove "next" if not needed
  },
  "scripts": {
    "start": "craco start",
    "build": "craco build",
    "postbuild": "npx rimraf docs/* && cp -r build/* docs/",
    "test": "craco test --env=jsdom",
    "eject": "craco eject"
  },
  "engines": {
    "node": ">=18.x"
  },
  "browserslist": { ... },
  "devDependencies": {
    "react-scripts": "^5.0.1"
  }
}
